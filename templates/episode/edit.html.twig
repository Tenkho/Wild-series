{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <h1>Editer un Episode</h1>
    <div class="container">
        {{ include('episode/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('episode_index') }}"><button class="btn btn-secondary">Retour à la liste</button></a>

        {{ include('episode/_delete_form.html.twig') }}
    </div>
{% endblock %}
