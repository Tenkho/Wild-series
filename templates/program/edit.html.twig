{% extends 'base.html.twig' %}

{% block title %}Editer la Série{% endblock %}

{% block body %}
    <h1>Editer la série</h1>

    <div class="container">
        {{ include('program/_form.html.twig', {'button_label': 'Enregistrer'}) }}

        <a href="{{ path('program_index') }}"><button class="btn btn-secondary">Retour à la liste</button></a>

        {{ include('program/_delete_form.html.twig') }}
    </div>
{% endblock %}
