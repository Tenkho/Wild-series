{% extends 'base.html.twig' %}

{% block title %}FormComment{% endblock %}

{% block body %}
    <div class="block-main">
        <div class="form-group">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">{{ button_label|default('Ajouter le Commentaire') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}