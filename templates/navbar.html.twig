<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Wild-Series</a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/wild/">Séries<span class="sr-only">(current)</span></a>
            </li>
        </ul>
        {% if app.user %}
            {{ app.user.email }}
            <a href="{{ path('app_logout') }}"><button class="btn btn-secondary ml-2 mt-0">Se déconnecter</button></a>
        {% else %}
            <a href="{{ path('app_login') }}"><button class="btn btn-secondary ml-2 mt-0">Se connecter</button></a>
        {% endif %}
    </div>
</nav>
